





 * Color setup dialog is terrible. 
 * Arch/Red Hat instructions
 * When closing using the window close button, there is no cleanup.



Done

 * About page should render a lot faster. 
 * Help menu seems to be too big.
 * since MapV is the default, it should be the first option in the visualisation menu
 * Input is suspended during expand/initial animation but not collapse animation.
 * DiscV expand/collapse animations should be faster.
 * In TreeV, text often blinks and is partially visible.
 * In TreeV mode, initial animation doesn't show. If switching when already expandedd, animation is very very slow and sometimes random.


Unsolvable for now

 * In DiscV, discs render underneath other discs. 
 * In TreeV, Expand All can be flickery. 
